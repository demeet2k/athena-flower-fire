# C3_RUNTIME↔C7_CORPUS Bridge — Inner/Outer Cell Edge

**Type**: Inner-outer cell bridge (tesseract w-axis)

This edge connects the outer cube cell C3_RUNTIME to the inner cube cell C7_CORPUS.
- Outer cube: expansion path (chapters unfold from kernel to runtime)
- Inner cube: compression path (deployment → inverse appendixes → corpus seed)

The bridge represents the 1/8 lift law: content in C3_RUNTIME expands what
content in C7_CORPUS compresses, and vice versa.
