# C0_KERNEL↔C4_DEPLOYMENT Bridge — Inner/Outer Cell Edge

**Type**: Inner-outer cell bridge (tesseract w-axis)

This edge connects the outer cube cell C0_KERNEL to the inner cube cell C4_DEPLOYMENT.
- Outer cube: expansion path (chapters unfold from kernel to runtime)
- Inner cube: compression path (deployment → inverse appendixes → corpus seed)

The bridge represents the 1/8 lift law: content in C0_KERNEL expands what
content in C4_DEPLOYMENT compresses, and vice versa.
